# Python Development Environment

当前工作区已配置为 Python 开发环境。

推荐步骤：
1. 运行 `Tasks: Create Python venv` 创建虚拟环境。
2. 运行 `Tasks: Install requirements` 安装依赖。
3. 打开 `main.py`，使用调试配置 `Python: Current File` 运行代码。
